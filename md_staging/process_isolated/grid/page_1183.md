```html
<!-- 
source: image
domain: syncfusion-sdk
task: pdf-ocr-to-markdown
language: en (keep original; do not translate)
source_filename: page_1183.jpeg
document_name: grid
page_number: 1183
page_id: grid#page_1183
product: Syncfusion Winforms
version: 11.4.0.26
timestamp: 2025-08-09T05:37:00Z
fidelity: lossless
-->

# Essential Grid for Windows Forms

## Overview
- Describes the `VisualTheme` method, its parameters, and usage for applying themes in a Windows Forms grid control.
- Explains how to add the `SkinManager` control to a Windows Forms application.

## Content

### Method Table

| Method                      | Description                           | Parameters                                                                 | Return Type |
|-----------------------------|---------------------------------------|--------------------------------------------------------------------------|-------------|
| `VisualTheme(Control, VisualTheme)` | Specifies the parent control and the theme to be applied. | **Overloads:**<br>• `(Control, String)`<br>• `(Control, VisualTheme)` | Void       |

### Sample Link

For more examples:

`<Sample installation Location>\Syncfusion\EssentialStudio\<Version Number>\Windows\Grid.Windows\Samples\2.0\Appearance\SkinManager Demo`

#### 4.13.1 Adding Skin Manager to an Application

You can add the `SkinManager` control to one of the controls in your form or to the entire control as needed by specifying the root control. You can specify the root control using the **Control** property.

To add the `SkinManager` control to one of the controls, specify the selected control as the root control. The following code illustrates this, where the control is styled with `Office2010Blue` theme.

**C#**
```csharp
SkinManager.SetVisualStyle(this.gridControl, VisualTheme.Office2010Blue);
```

**VB**
```vbnet
SkinManager.SetVisualStyle(Me.gridControl, VisualTheme.Office2010Blue)
```

To add `Skin Manager` to the entire form, specify the form as the root control. The following code illustrates this.

**C#**
```csharp
SkinManager.SetVisualStyle(this, VisualTheme.Office2010Blue);
```

**VB**
```vbnet
SkinManager.SetVisualStyle(Me, VisualTheme.Office2010Blue)
```

## API Reference

- **Namespace:** Syncfusion.Windows.Forms
- **Class:** SkinManager
- **Method:** `SetVisualStyle(Control, VisualTheme)`
  - **Parameters:** 
    - `Control`: The root control to which the theme is applied.
    - `VisualTheme`: The theme to be applied to the control.
  - **Return Type:** Void

<!-- tags: [Syncfusion Winforms, SkinManager, VisualTheme, Grid, Method Table, Sample Link] keywords: [Syncfusion, Windows Forms, Grid, VisualTheme, SkinManager, Office2010Blue, Control] -->
```