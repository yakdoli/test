```html
<!-- 
source: image
domain: syncfusion-sdk
task: pdf-ocr-to-markdown
language: en (keep original; do not translate)
source_filename: page_368.jpeg
document_name: grid
page_number: 368
page_id: grid#page_368
product: Syncfusion Winforms
version: 11.4.0.26
timestamp: 2025-08-09T06:12:48Z
fidelity: lossless
-->

# Essential Grid for Windows Forms

## Overview
- Percent must be >= 0 and <= 1.
- TRIMMEAN rounds off the number of excluded data points down to the nearest multiple of 2. If percent = 0.1, 10 percent of 30 data points equals 3 points. For symmetry, TRIMMEAN excludes a single value from the top and bottom of the data set.

## Content
### Trunc Function
**4.1.4.6.6.249 TRUNC**

The Trunc function truncates a supplied number to a specified number of decimal places.

**Syntax**

TRUNC(number, num_digits), where:

- **number** is the number you want to truncate.
- **num_digits** is an optional argument that specifies the number of decimal places to truncate the supplied number to. The default value is 0.

**Remarks**

- TRUNC and INT are similar in that both return integers. TRUNC removes the fractional part of the number. INT rounds numbers down to the nearest integer based on the value of the fractional part of the number. INT and TRUNC are different only when using negative numbers: TRUNC(-4.3) returns -4 but, INT(-4.3) returns -5 because -5 is the lower number.

### True Function
**4.1.4.6.6.250 True**

The True function returns the logical value when the given string value is true.

**Syntax:**

True(stringvalue)

where:

- **stringvalue** is to provide any text value or empty string.

### Upper Function
**4.1.4.6.6.251 Upper**

The Upper function converts all characters in a text string to uppercase.

**Syntax:**

## API Reference (if applicable)
- Note: The specific API reference details are not explicitly provided in the image, so this section is left as a placeholder.

## Code Examples (multi-language supported)
- The image does not contain any code examples, so this section is left as a placeholder.

## Page-level Navigation/TOC (if applicable)
- This section is not applicable based on the provided content.

## Cross References
- See also: TRIMMEAN, INT, string manipulation functions.

## RAG Annotations
<!-- tags: [Syncfusion, Windows Forms, Truncate, True, Upper, functions, numerical processing, string manipulation] keywords: [TRUNC, INT, terminal, upper case, decimal places, true logical value] -->
```
```