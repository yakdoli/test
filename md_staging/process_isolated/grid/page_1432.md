```html
<!-- 
source: image
domain: syncfusion-sdk
task: pdf-ocr-to-markdown
language: en (keep original; do not translate)
source_filename: page_1432.jpeg
document_name: grid
page_number: 1432
page_id: grid#page_1432
product: Syncfusion Winforms
version: 11.4.0.26
timestamp: 2025-08-09T05:55:07Z
fidelity: lossless
-->

# Essential Grid for Windows Forms

```csharp
gridGroupingControl.PrintRowHeader = true;
```

## Printing Horizontal Lines

You can print the horizontal lines using the `PrintHorizontalLines` property.

The following code illustrates how to print the horizontal lines in `GridControl`:

```csharp
gridControl.PrintHorizontalLines = true;
```

### Printing Horizontal Lines in GridDataBoundGrid

The following code illustrates how to print the horizontal lines in `GridDataBoundGrid`:

```csharp
gridDataBoundGrid.PrintHorizontalLines = true;
```

### Printing Horizontal Lines in GridGrouping Control

The following code illustrates how to print the horizontal lines `GridGrouping` control:

```csharp
gridGroupingControl.PrintHorizontalLines = true;
```

## Printing Vertical Lines

You can print the vertical lines using the `PrintVerticalLines` property.

The following code illustrates how to print the vertical lines in `GridControl`:

```csharp
gridControl.PrintVerticalLines = true;
```

### Printing Vertical Lines in GridDataBoundGrid

The following code illustrates how to print the vertical lines in `GridDataBoundGrid`:

```csharp
gridDataBoundGrid.PrintVerticalLines = true;
```

### Printing Vertical Lines in GridGrouping Control

The following code illustrates how to print the vertical lines `GridGrouping` control:

```csharp
gridGroupingControl.PrintVerticalLines = true;
```

### Grid Model Options

This section provides information on how to write syntax for the following `GridModel` Options:

- **ActivateCurrentCellBehavior**

---
© 2013 Syncfusion. All rights reserved. 1432 | Page

<!-- tags: [syncfusion, winforms, grid, gridcontrol, griddataboundgrid, gridgrouping, printing, horizontal lines, vertical lines, gridmodel, activatecurrentcellbehavior] keywords: [printrowheader, printhorizontallines, printverticallines, gridcontrol, griddataboundgrid, gridgroupingcontrol, gridmodel] -->
``` 
